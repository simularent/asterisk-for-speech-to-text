#!/bin/sh
set -e

if which pyclean >/dev/null 2>&1; then
    pyclean -p google-cloud-sdk
else
    dpkg -L google-cloud-sdk | grep \.py$ | while read file
    do
        rm -f "${file}"[co] > /dev/null
    done
fi

rm -rf /usr/lib/google-cloud-sdk/.install/static_completion > /dev/null
